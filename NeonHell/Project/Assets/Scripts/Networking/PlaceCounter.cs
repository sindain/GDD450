using UnityEngine;
using System.Collections;

public class PlaceCounter : MonoBehaviour {
    //This is the server variable for checking how many players have finished the race
    public int placeCounter;
	// Use this for initialization
	void Start () {
        placeCounter = 0;
	}
	
	// Update is called once per frame
	void Update () {

	}
}
